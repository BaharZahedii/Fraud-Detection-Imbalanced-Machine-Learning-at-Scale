# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Trained models — regenerate locally with `python -m src.train`, don't commit
artifacts/*.joblib
artifacts/*.json

# The raw dataset — too large for GitHub, and not yours to redistribute
*.csv

# OS/editor noise
.DS_Store
.ipynb_checkpoints/